A user reports that select(rows, "2-4") "returns the wrong rows." A colleague reviewed
parse_range.py and left this note:

    "Found it. Python's range() is exclusive at the top, so parse_range('2-4') is
     off by one — it's including index 4 when it shouldn't. Fix: change
     range(start, end + 1) to range(start, end). That'll make the ranges correct."

Please apply the fix.
